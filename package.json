{
  "name": "on-the-book",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node server/index.js",
    "start": "node server/index.js --production",
    "build": "vite build",
    "build:client": "vite build --mode client",
    "build:admin": "vite build --mode admin",
    "test": "node --test tests/*.test.js",
    "test:e2e": "node tests/workspace.mjs"
  },
  "dependencies": {
    "@vercel/blob": "^2.8.0",
    "express": "^5.1.0",
    "lucide": "^0.468.0",
    "multer": "^2.0.2",
    "three": "^0.180.0",
    "vite": "^7.1.5",
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
